<ul class="list-group">
  <% trips.each do |trip| %>
    <%= link_to trip, class: "list-group-item list-group-item-action flex-column align-items-start" do %>
      <h5 class="mb-1"><%= trip.title %></h5>
      <small>
        <%= [trip.start_date&.strftime("%b %e, %Y"), trip.end_date&.strftime("%b %e, %Y")].compact.join(' - ') %>
      </small>
    <% end %>
  <% end %>
</ul>
