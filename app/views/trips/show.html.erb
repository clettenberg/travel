<p id="notice"><%= notice %></p>

<h1><%= @trip.title %> <small><%= link_to 'Edit', edit_trip_path(@trip) %></small></h1>


<h3>Places</h3>
<% @trip.pois.each do |poi| %>
  <%= link_to poi, poi.title %>
<% end %>

<%= link_to 'Back', trips_path %>
