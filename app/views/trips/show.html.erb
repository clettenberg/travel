<p id="notice"><%= notice %></p>

<h1><%= @trip.title %> <small class="text-muted"><%= link_to 'edit', edit_trip_path(@trip) %></small></h1>


<h3>Places</h3>

<% if @trip.pois.any? %>
  <%= render "poi_list", pois: @trip.pois %>
<% else %>
  <p>No Places</p>
<% end %>

<br>
<%= link_to 'Back', trips_path, class: "btn btn-primary" %>
