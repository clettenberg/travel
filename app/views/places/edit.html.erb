<h1>
  Edit
  <%= link_to 'Cancel', place_path(@place) %>
</h1>
<%= render 'form', trip: @trip, place: @place %>
<%= link_to 'Delete', place_path(@place), method: :delete, data: { confirm: "Are you sure you want to delete this Place?\nYou will lose all of the data associated with it." } %>
